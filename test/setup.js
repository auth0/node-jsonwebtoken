// Jest setup file - loaded before all tests

// Add any global test setup here
// For example, you can set global test timeouts, configure test utilities, etc.

// Increase default timeout for async operations if needed
jest.setTimeout(10000);